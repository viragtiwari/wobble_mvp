# This is an example webapp.io configuration for NodeJS
FROM vm/ubuntu:20.04
# To note: Layerfiles create VMs, *not* containers!

# Install node
RUN curl -sL https://deb.nodesource.com/setup_16.x | bash && \
    apt-get install -y nodejs

# If you use a different version of Node.js, change the setup_X.x to your version

# Set the working directory to /app
WORKDIR /app

# Copy your source code into the VM
COPY . .

# Install dependencies
RUN npm install

# Expose the port your app runs on
EXPOSE 5000

# Start your app
CMD ["npm", "start"]
